name: app

on:
  push:
    branches: [ "main" ]
    paths:
      - pack/version.txt
      
jobs:
  build:
    permissions: write-all
    runs-on: windows-latest 
    env:
      EnvData: hello
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: SetVersion
      id: SetVersion
      run: |
        echo "version=$(cat ./pack/version.txt)" >> $ENV:GITHUB_OUTPUT
        echo "xx=yy" >> $ENV:GITHUB_OUTPUT

    - name: GetVersion
      run: write-output 123 $GITHUB_OUTPUT ${{ steps.SetVersion.outputs.version }} "${{ steps.SetVersion.outputs.version }}" 234 ${{ steps.SetVersion.outputs.xx }} 666

    - name: Install .NET Core
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v2

    - name: Invoke
      run: ./pack/test.bat

    - name: release
      uses: ncipollo/release-action@v1
      with:
        tag: ${{ steps.SetVersion.outputs.version }}
        artifacts: "pack/files/*.txt"
        bodyFile: "pack/body.md"